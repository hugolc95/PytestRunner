[pytest]
# Workspace de démonstration chargé par défaut dans l'interface.
# Les tests internes du projet sont lancés explicitement avec: pytest tests
# Ne pas remettre "testpaths = tests", sinon testSuite1 est masqué dans la GUI.
testpaths = testSuite1
python_files = test_*.py
addopts = -ra
