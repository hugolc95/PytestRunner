PySide6==6.11.2
PyYAML==6.0.3
colorama==0.4.6
qtawesome==1.4.2

# GUI/runtime x64 only. Pytest itself is intentionally not required by the
# packaged UI: test execution is delegated to the external interpreter chosen
# in the Runner (the target can therefore remain Python 3.13 x86).
